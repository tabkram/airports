'use strict';

describe('myApp.query module', function() {

  beforeEach(module('myApp.query'));

  describe('query controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var queryCtrl = $controller('queryCtrl');
      expect(queryCtrl).toBeDefined();
    }));

  });
});